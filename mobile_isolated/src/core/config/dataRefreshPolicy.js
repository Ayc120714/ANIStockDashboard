/** Show cached screen data instantly; refresh from API when stale or during live session. */
export const ALWAYS_FETCH_FROM_DB = false;

/** Background refresh while app is open (ms). */
export const APP_SHELL_REFRESH_MS_LIVE = 45_000;
export const APP_SHELL_REFRESH_MS_CLOSED = 5 * 60_000;
